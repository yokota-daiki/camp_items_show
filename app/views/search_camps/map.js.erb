let marker = []

function(){
  for (var i = 0; i < gon.campLat.length; i++) {
    let pos = new google.maps.LatLng({
      lat: gon.campLat[i],
      lng: gon.campLng[i]
    });

    marker[i] = new google.maps.Marker({
      map: mapInstance,
      position: pos
    });
  }
}()

